"""
main.py — imports the geometry module and uses its functions.

Run with: python main.py
"""

import geometry

print("Q2: Using the geometry module")
print("Area of circle (r=5):", geometry.area_of_circle(5))
print("Area of rectangle (4x6):", geometry.area_of_rectangle(4, 6))
print("Area of triangle (base=8, height=5):", geometry.area_of_triangle(8, 5))
